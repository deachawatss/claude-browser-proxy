# Screenshots

Dropped here by `scripts/shot.sh`. The extension cannot write outside the
browser download directory (chrome.downloads refuses absolute paths), so the
script captures into Downloads and moves the PNG here.

These are not committed automatically — add the ones a doc actually references.
